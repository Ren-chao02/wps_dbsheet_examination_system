# 学生信息管理功能 — 待完成任务

## Context

按照「学生信息管理功能实施方案」已完成了大部分开发工作，包括 Prisma Schema 扩展、后端 CRUD API（学生/组织架构/邀请/审批）、前端全部页面组件、批量导入导出等功能。

经过全面的代码审查，发现 **5 个问题** 需要修复才能让模块完整可用。

---

## Task 1：后端 — 添加重置密码端点

**修改文件：** `packages/server/src/routes/students.ts`

在 `DELETE` 路由之前添加：

```
PUT /api/students/:id/reset-password
```

- 鉴权：admin + teacher
- 逻辑：检查学生是否存在，使用 `generateDefaultPassword(studentId)` 生成新密码（学号后6位），bcrypt 哈希后更新 User
- 返回：`{ message: '密码已重置为默认密码' }`

---

## Task 2：后端 — updateStudentSchema 添加 accountStatus 字段

**修改文件：** `packages/server/src/routes/students.ts`

在 `updateStudentSchema` 中添加：

```typescript
accountStatus: z.enum(['ACTIVE', 'INACTIVE', 'PENDING_APPROVAL']).optional(),
```

并在 `prisma.user.update` 的 `data` 中传递 `accountStatus`：

```typescript
...(data.accountStatus !== undefined && { accountStatus: data.accountStatus }),
```

---

## Task 3：前端 — StudentManagement 添加删除学生按钮

**修改文件：** `packages/client/src/pages/teacher/StudentManagement.tsx`

在操作列（第 268 行附近）添加一个删除按钮：
- 使用 `Popconfirm` 确认删除
- 只有 admin 角色可以看到（使用 `useAuthStore` 获取当前用户角色）
- 调用 `DELETE /api/students/:id`
- 删除成功后刷新列表

---

## Task 4：前端 — StudentImport 模板描述修正

**修改文件：** `packages/client/src/pages/teacher/StudentImport.tsx`

第 96 行的模板字段描述从：
```
模板包含字段：用户名、密码、学号、姓名、性别、手机号、邮箱、院系编码、专业编码、班级编码
```
改为与实际导出模板一致的：
```
模板包含字段：学号、姓名、性别、手机号、邮箱、班级编码
```

---

## Task 5：App.tsx — 删除重复的 /join/:code 路由

**修改文件：** `packages/client/src/App.tsx`

第 109 行的占位路由与第 95 行的正式 `StudentJoinPage` 路由重复，且 React Router v6 中第 109 行会先匹配，导致 `StudentJoinPage` 永远不会渲染。

删除第 109 行：
```
<Route path="/join/:code" element={<div>学生申请 - 开发中</div>} />
```

---

## 验证方案

1. **重置密码**：通过前端点击"重置密码"按钮，确认返回成功，用新密码（学号后6位）可登录
2. **启用/禁用**：点击启用/禁用按钮，确认学生账号状态改变，且禁用后无法登录
3. **删除学生**：admin 账户点击删除按钮，确认学生从列表中消失且数据库记录删除
4. **模板描述**：打开批量导入页面第一步，确认提示文字与实际模板一致
5. **邀请链接**：访问 `/join/{有效code}`，确认显示的是 `StudentJoinPage` 组件而非占位文本
